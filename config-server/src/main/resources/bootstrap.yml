---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nykykof/ConfigData/
      name: config-server
  application:
    name: config-server
          
server:
  port: 8001
  
management:
  security:
    enabled: false  